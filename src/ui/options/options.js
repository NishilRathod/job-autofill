/**
 * Options page controller.
 *
 * Phase 2 builds the full profile editor here, backed by src/storage/store.js.
 * The page is currently static, but it is loaded as a module so that adding
 * those imports later needs no change to options.html.
 */
